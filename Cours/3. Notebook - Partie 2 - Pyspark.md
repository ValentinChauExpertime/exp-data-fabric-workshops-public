<link rel="stylesheet" type="text/css" href="styles.css">

# Notebook : Traitement de données des fichiers .json via PySpark
Le traitement des fichiers JSON peut être rendu complexe selon l'outil utilisé.<br>
En effet, bien que le JSON soit un format populaire et largement utilisé en raison de sa lisibilité pour les humains et de sa flexibilité pour représenter des données complexes, son traitement peut être particulier en raison de sa structure flexible, de l'absence de schéma rigide. 
<table>
<tr>
<td>

**Exemple de format 1**
```json
{
    "site": "Machu Picchu",
    "pays": "Pérou",
    "année_inscription": 1983,
}
```

**Exemple de format 2**

```
[
    {
        "site": "Grande Muraille de Chine",
        "pays": "Chine",
        "année_inscription": 1987
    },
    {
        "site": "Pyramides de Gizeh",
        "pays": "Égypte",
        "année_inscription": 1979
    },
    ...
]
```
</td>

<td>

**Exemple de format 3**
```
{
    {
        "site": "Acropole d'Athènes",
        "pays": "Grèce",
        "année_inscription": 1987,
        "principaux_monuments": [
            "Parthénon",
            "Érechthéion",
            "Temple d'Athéna Nikè",
            "Théâtre de Dionysos"
        ]
    },
    {
        "site": "Colisée de Rome",
        "pays": "Italie",
        "année_inscription": 1980
    }
}
```
</td>
</tr>
</table>

Dans la suite du travail, nous allons importer les fichiers annuels des sites inscrits au patrimoine mondial de l'UNESCO.
> **Note :**<br> Ce notebook sera en deux étapes. Une première pour l'import des fichiers .json sous forme de DataFrame et la deuxième partie qui elle sera liée aux transformations de données.

# 1. [Pratique] Importer les fichiers .JSON sous forme de DataFrame Spark
## Bibliothèques
Dans ce notebook, on va utiliser les bibliothèques suivantes :
```python
from pyspark.sql.functions import udf, col, when
import pyspark.sql.types as T
#from pyspark.sql.types import ArrayType, DoubleType, IntegerType, StringType
```
## 1.1. Exemple sur un seul fichier
Sur Pyspark, cela se fait très simplement. On va tirer parti de la génération automatique de code pour avancer encore plus rapidement.
1. Dans votre **Lakehouse Bronze**, aller dans **Fichiers/World Heritage Committee** <br>
    <img src="https://github.com/Expertime/exp-data-fabric-workshops/blob/main/Images/Notebook/Notebook-PySpark-Get-Json-file.png" alt="" width="35%"/> <br>

2. Sélectionner pour **WHC_1978.json** (ou n'importe quel autre fichier de ce dossier) puis dans le menu **...**, sélectionnez **Charger les données** > **Spark**. Le code ci-dessous devrait être ajouté à votre notebook. Exécuter ensuite la cellule:<br>
    ``` python
    df = spark.read.option("multiline", "true").json("Files/World Heritage Committee/WHC_1978.json")
    # df now is a Spark DataFrame containing JSON data from "Files/World Heritage Committee/WHC_1978.json".
    display(df)
    ```

    _Output :_<br>
    <img src="https://github.com/Expertime/exp-data-fabric-workshops/blob/main/Images/Notebook/Notebook-PySpark-read-json-output1.png" alt="" width="70%"/> <br>
3. On va applatir les éléments de **Informations complementaires**
    ``` python
    df = df.select("ID", "Site", "Description", "`Informations complementaires`.*")
    display(df)
    ```
    _Output :_

## 1.2. Exemple sur l'ensemble des fichiers .json
1. Remplacer *WHC_1978* par **\*** afin de lire tous les fichiers **.json** dans le dossier et éxécuter encore la cellule:<br>
    ``` python
    df = spark.read.option("multiline", "true").json("Files/World Heritage Committee/*.json")
    # df now is a Spark DataFrame containing JSON data from "Files/World Heritage Committee/WHC_1978.json".
    display(df)
    ```

2. On va applatir les éléments de **Informations complementaires**
    ``` python
    df_new = df.select("ID", "Site", "Description", "`Informations complementaires`.*")
    display(df_new)
    ```
    
3. Pour garantir que les valeurs manquantes (NaN) ne soient pas interprétées comme des chiffres lors de la manipulation des données, même si la colonne est de type chaîne de caractères, nous devons remplacer les valeurs NaN par None. Cela peut être réalisé avec la méthode `replace` :
    ```python
    import numpy as np
    df_new = df_new.replace(np.nan, None)
    ```
