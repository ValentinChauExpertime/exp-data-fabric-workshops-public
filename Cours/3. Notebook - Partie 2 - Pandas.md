<link rel="stylesheet" type="text/css" href="styles.css">

- Manipulation de dictionnaires
- Boucles

# Notebook : Traitement de données des fichiers .json via Pandas
Le traitement des fichiers JSON peut être rendu complexe selon l'outil utilisé.<br>
En effet, bien que le JSON soit un format populaire et largement utilisé en raison de sa lisibilité pour les humains et de sa flexibilité pour représenter des données complexes, son traitement peut être particulier en raison de sa structure flexible, de l'absence de schéma rigide. 
<table>
<tr>
<td>

**Exemple de format 1**
```
{
    "site": "Machu Picchu",
    "pays": "Pérou",
    "année_inscription": 1983,
}
```

**Exemple de format 2**

```
[
    {
        "site": "Grande Muraille de Chine",
        "pays": "Chine",
        "année_inscription": 1987
    },
    {
        "site": "Pyramides de Gizeh",
        "pays": "Égypte",
        "année_inscription": 1979
    },
    ...
]
```
</td>

<td>

**Exemple de format 3**
```
{
    {
        "site": "Acropole d'Athènes",
        "pays": "Grèce",
        "année_inscription": 1987,
        "principaux_monuments": [
            "Parthénon",
            "Érechthéion",
            "Temple d'Athéna Nikè",
            "Théâtre de Dionysos"
        ]
    },
    {
        "site": "Colisée de Rome",
        "pays": "Italie",
        "année_inscription": 1980
    }
}
```
</td>
</tr>
</table>

Dans la suite du travail, nous allons importer les fichiers annuels des sites inscrits au patrimoine mondial de l'UNESCO.


# 1. [Pratique] Importer les fichiers .JSON sous forme de DataFrame Pandas
> Si vous maitrisez les notions plus haut, passer directement au point **1.2**
## Bibliothèques
Dans ce notebook, on va utiliser les bibliothèques suivantes :
```python
from pyspark.sql.functions import udf, col, when#, lit
import pyspark.sql.types as T
#from pyspark.sql.types import ArrayType, DoubleType, IntegerType, StringType
```

## 1.1 Pas à pas
Pour la suite, vous pouvez ajouter des nouvelles cellules et copier les codes ci-dessous afin d'observer le résultat; des commentaires seront faits si nécessaire.
1. Lecture d'un fichier
    ``` python
    import json

    f = open("/lakehouse/default/" +"Files/World Heritage Committee/WHC_1978.json","r", encoding='utf-8')
    content = json.load(f)
    print("Lecture terminée")
    print( "Type de la variable \"content\" : ", type(content) )
    ```
    _Output :_
    ```
    Lecture terminée
    Type de la variable "content" :  <class 'list'>
    ```

2. Afficher le contenu du fichier
    ``` python
    print( content )
    # display (content) # si vous utilisez cela le notebook affichera le resultat sous forme de dataframe
    ```
    _Output :_
    ```
    [
        {
            'ID': 1,
            'Site': 'Îles Galápagos',
            'Description': 'source: UNESCO/CPE\nLa description est disponible sous licence CC-BY-SA IGO 3.0\n\nSituées dans l’océan Pacifique, à environ 1000 km du continent sud-américain, ces dix-neuf îles et la réserve marine qui les entoure constituent un musée et un laboratoire vivants de l’évolution uniques au monde. Au confluent de trois courants océaniques, les Galápagos sont un creuset d’espèces marines. L’activité sismique et le volcanisme toujours en activité illustrent les processus qui ont formé ces îles. Ces processus, ainsi que l’isolement extrême de ces îles, ont entraîné le développement d’une faune originale - notamment l’iguane terrestre, la tortue géante et de nombreuses espèces de pinsons qui inspira à Charles Darwin sa théorie de l’évolution par la sélection naturelle\xa0à la suite de sa visite en 1835.',
            'Informations complementaires': {
                "Date d'inscription": '1978',
                'Significant modifications to the boundaries': '2001',
                'Critères': '(vii)(viii)(ix)(x)',
                'Bien': '14 066 514 ha',
                'Dossier': '1bis',
                'Coordonnées_en_DMS': '0 41 23.496 N 90 30 4.748 W'
            }
        },
        {...}
   ]
   ```
   Il s'agit du contenu du fichier .json qui a été importé.

3. Explorer le contenu du fichier
    ``` python
    print( "Nombre d'éléments dans \"content\":", len(content) )
    print( "Type de la variable \"content\":", type(content) )
    print()

    print( "Type de la variable \"content[0]\":", type(content[0]) )
    print( "Clés du dictionnaire \"content[0]\":", content[0].keys() )

    for x in content[0].keys():
        print( "{}: {}".format(x, type(content[0][x])) )
    print()

    print( "Clés de \"Informations complementaires\":", content[0]["Informations complementaires"].keys() )
     ```

    _Output :_
    ```
    Nombre d'éléments dans "content": 12
    Type de la variable "content": <class 'list'>

    Type de la variable "content[0]": <class 'dict'>
    Clés du dictionnaire "content[0]": dict_keys(['ID', 'Site', 'Description', 'Informations complementaires'])
    ID: <class 'int'>
    Site: <class 'str'>
    Description: <class 'str'>
    Informations complementaires: <class 'dict'>

    Clés de "Informations complementaires": dict_keys(["Date d'inscription", 'Significant modifications to the boundaries', 'Critères', 'Bien', 'Dossier', 'Coordonnées_en_DMS']) 
    ```

4. On convertit le contenu du fichier .json en DataFrame pandas à l'aide de `json_normalize`
    ``` python
    df = pd.json_normalize(content)
    display( df.info() )
    display( df )
    ```
    _Output :_
    ```
    <class 'pandas.core.frame.DataFrame'>
    RangeIndex: 12 entries, 0 to 11
    Data columns (total 11 columns):
    #   Column                                                                                   Non-Null Count  Dtype 
    ---  ------                                                                                   --------------  ----- 
    0   ID                                                                                       12 non-null     int64 
    1   Site                                                                                     12 non-null     object
    2   Description                                                                              12 non-null     object
    3   Informations complementaires.Date d'inscription                                          12 non-null     object
    4   Informations complementaires.Significant modifications to the boundaries                 2 non-null      object
    5   Informations complementaires.Critères                                                    12 non-null     object
    6   Informations complementaires.Bien                                                        10 non-null     object
    7   Informations complementaires.Dossier                                                     12 non-null     object
    8   Informations complementaires.Coordonnées_en_DMS                                          12 non-null     object
    9   Informations complementaires.Zone tampon                                                 4 non-null      object
    10  Informations complementaires.Année d'inscription de la modification mineure des limites  4 non-null      object
    dtypes: int64(1), object(10)
    memory usage: 1.2+ KB
    ```
    <img src="Images/Notebook/Notebook-result-whs-details-mini-dataframe.png" alt="" width="80%"/> <br>

## 1.2. Création du dataframe complet
Vous avez compris comment :
- lire un fichier .json
- utiliser la méthode `json_normalize` pour aplatir les données imbriquées afin de transformer les données JSON en dataframe

1. Ici, nous allons utiliser une boucle pour répéter l'opération et ensuite combiner les résultats en un seul dataframe.
    ``` python
    racine_dossier_Lakehouse = "/lakehouse/default/" +"Files/World Heritage Committee"
    lst = []
    #for var_annee in ["1978","1979"]:
    for var_annee in range(1978,2024):
        print(var_annee)
        try:
            f = open(f"{racine_dossier_Lakehouse}/WHC_{var_annee}.json","r", encoding='utf-8')
            content = json.load(f)
            sub_df = pd.json_normalize(content)
            sub_df["Source"] = f"WHC_{var_annee}.json" # On ajoute une colonne pour indiquer le fichier source
            lst.append(sub_df)
        except:
            print(f"Pas de fichier pour l'année {var_annee}")

    df = pd.concat(lst).reset_index(drop=True)
    display( df.info() )
    df.head()
    ```

2. Pour garantir que les valeurs manquantes (NaN) ne soient pas interprétées comme des chiffres lors de la manipulation des données, même si la colonne est de type chaîne de caractères, nous devons remplacer les valeurs NaN par None. Cela peut être réalisé avec la méthode `replace` :
    ```python
    import numpy as np
    df = df.replace(np.nan, None)
    ```
### Version alternative
On utilise la méthode `listdir` pour obtenir la liste des fichiers du dossier **World Heritage Committee** (vu que **TOUS** les fichiers dans ce dossier sont similaires)
``` python
import os
import json
import pandas as pd
import numpy as np

racine_dossier_Lakehouse = "/lakehouse/default/" +"Files/World Heritage Committee"
folder_content = os.listdir(racine_dossier_Lakehouse) # Liste des fichiers dans le dossier
lst = []

for json_file in folder_content:
    print(json_file)
    f = open(f"{racine_dossier_Lakehouse}/{json_file}","r", encoding='utf-8')
    content = json.load(f)
    sub_df = pd.json_normalize(content)
    sub_df["Source"] = json_file # On ajoute une colonne pour indiquer le fichier source
    lst.append(sub_df)

df = pd.concat(lst).reset_index(drop=True).replace(np.nan, None)
display( df.info() )
df.head()
```
