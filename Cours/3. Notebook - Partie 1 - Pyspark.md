<link rel="stylesheet" type="text/css" href="styles.css">

```
- Dataframe PySpark : 
    - Création à partir d'un fichier .csv ou .xlsx
    - Analyse de la structure
    - Data transformations primaires
- Exploration des outputs du Notebook MS Fabric
- Création d'une table delta dans un Lakehouse MS Fabric
```

# Notebook : Premières manipulations
# 1. [Pratique] Importer le fichier Pays.csv dans le notebook avec PySpark
1. Dans le menu **...**, sélectionner pour **Pays.csv**, sélectionnez **Charger les données** > **Spark**. Une nouvelle cellule de code contenant le code suivant devrait être ajoutée au notebook :<br>
    ``` python
    df = spark.read.format("csv").option("header","true").load("Files/Pays.csv")
    # df now is a Spark DataFrame containing CSV data from "Files/Pays.csv".
    display(df)
    ```
    
2. Cliquer sur le bouton **&#9655;** à gauche de la cellule pour l'éxcuter  (**Raccourci :** Vous pouvez également faire **CTRL** + **Entrée**)
3. En dessous de la cellule nouvellement éxécutée vous pourrez voir le résultat d'affichage
    <img src="https://github.com/Expertime/exp-data-fabric-workshops/blob/main/Images/Notebook/Notebook-Pays-csv-pyspark.png" alt="" width="80%"/>
4. Le format du texte n'est pas le bon. Compléter le code autogénéré en ajoutant dans `load`, les paramètres suivants `encoding='ISO-8859-1', sep=";"` puis réexécuter le code
    ``` python
    df = spark.read.format("csv").option("header","true").load("Files/Pays.csv", encoding='ISO-8859-1', sep=";")
    # df now is a Spark DataFrame containing CSV data from "Files/Pays.csv".
    display(df)
    ```
    Le format est à présent satisfaisant
    <img src="https://github.com/Expertime/exp-data-fabric-workshops/blob/main/Images/Notebook/Notebook-Pays-csv-good-import.png" alt="" width="80%"/> <br>
5. On vérifie le schéma du dataframe Spark.
    ``` python
    df.printSchema()
    ```
    _Output :_
    ```json
    root
    |-- code_region: string (nullable = true)
    |-- region: string (nullable = true)
    |-- code_pays: string (nullable = true)
    |-- pays: string (nullable = true)
    ```

6. Sauvegarder le dataframe dans le **Lakehouse Silver**
    ``` python
    spark_df = spark_df.toDF( *(column.replace(' ', '_') for column in spark_df.columns) ) # On retire les espaces des noms de colonnes
    spark_df.write.format("delta").mode("overwrite").saveAsTable("DE_LH_Silver.Pays")
    ```
    Une fois le code exécuté, aller dans le **Lakehouse Silver** afin de vérifier que la table a été créée.<br>
    <img src="https://github.com/Expertime/exp-data-fabric-workshops/blob/main/Images/Notebook/Notebook-result-pays.png" alt="" width="20%"/> <br>

# 2. [Pratique] Importer le fichier Liste_2023.xlsx dans le notebook avec PySpark
> **Remarque :** <br> Pour faciliter la lecture du notebook, vous pouvez créer un nouveau notebook pour faire les traitements de données dans ce fichier.

## Bibliothèques
Dans ce notebook, on va se servir des bibliothèques suivantes :
```python
import pandas as pd
from pyspark.sql.functions import col, concat, lit, when
from pyspark.sql.types import IntegerType, StringType, BooleanType
```
## Pratique
1. Créer une nouvelle cellule et copier le code suivant pour lire le fichier **Liste_2023.xlsx** sous forme de dataframe pandas:
    ``` python
    # import pandas as pd
    df = pd.read_excel('/lakehouse/default/Files/Liste_2023.xlsx', sheet_name='Sheet1', engine='openpyxl')
    df =spark.createDataFrame(pandas_df) # convertir le dataframe Pandas en dataframe PySpark

    display(df)
    ```
    > Vous avez sans doute remarqué qu'on est passé par `Pandas` pour lire le fichier *.xlsx*. avant de le convertir en dataframe Spark.<br>
    Sur `Spark` pour lire des fichiers au format *.xlsx*, on utilise le package `com.crealytics:spark-excel_X.XX:X.XX.X`. Actuellement ce package n'est pas installé par défaut (Il aurait fallu passer par la création d'un [**Environnement** dans Fabric](https://learn.microsoft.com/fr-fr/fabric/data-engineering/create-and-use-environment)).

2. On utilise les méthodes `describe()` (tester avec `summary()` également pour voir la différence) et `printSchema()` pour avoir quelques infos sur le dataframe.
    ```python
    df.describe().show()
    df.printSchema()
    ```
    _Output :_
    ```
    +-------+------------+--------------+--------------------+-----------------+
    |summary|        Pays|     Catégorie|                Site|               ID|
    +-------+------------+--------------+--------------------+-----------------+
    |  count|        1303|          1303|                1303|             1303|
    |   mean|        null|          null|                null|880.9293937068304|
    | stddev|        null|          null|                null|487.6983211586844|
    |    min| Afghanistan|      cultural| Sanctuaires du g...|                1|
    |    max|Îles Salomon|natural_danger|  ‘Uruq Bani Ma’arid|             1703|
    +-------+------------+--------------+--------------------+-----------------+

    root
    |-- Pays: string (nullable = true)
    |-- Catégorie: string (nullable = true)
    |-- Site: string (nullable = true)
    |-- Site délisté: boolean (nullable = true)
    |-- ID: long (nullable = true)
    ```
2. Pour créer une nouvelle colonne **URL**, on va se servir de la méthode `withColumn` (elle sert soit à créer une nouvelle colonne, soit à remplacer une colonne existante) ainsi que de fonctions natives à **SQL** via `pyspark.sql.functions` pour réaliser la concatenation de l'ID du site et de la racine de l'URL.
    ``` python
    from pyspark.sql.functions import col, concat, lit
    
    df = df.withColumn("URL", concat(lit('https://whc.unesco.org/fr/list/'),col("ID")) )
    # Afficher les 5 premieres lignes du dataframe
    display( df.limit(5) )
    ```

3. Dans la colonne **Catégorie**, selon que le site soit le suffixe *_danger* vient indique que le site est en péril. Pour continuer notre travail, nous allons dans un premier temps créer une nouvelle colonne contenant cette information puis nous allons uniformiser les valeurs la colonne **Catégorie** en faisant un remplacement de valeurs. <br>
Pour ajouter la colonne **En péril** dans le dataframe, qui indique si un site est en péril ou non, nous allons utiliser la méthode `withColumn`:

    ``` python
    df = df.withColumn( "En péril", when(col("Catégorie").contains('danger'),True).otherwise(False) )
    
    # Afficher les 5 premieres lignes du dataframe
    display( df.limit(5) )
    ```

4. Avant d'effectuer le remplacement des valeurs de la colonne **Catégorie** il faudrait avoir une idée du nombre de remplacements à effectuer (cela permet également d'utiliser une méthode de remplacement adéquate). Pour avoir cette liste de valeurs, on utilisera la **méthode** `distinct()`:
    ``` python
    df.select("Catégorie").distinct().show()
    ```
    _Output :_
    ```
    +---------------+
    |      Catégorie|
    +---------------+
    |       cultural|
    |          mixed|
    | natural_danger|
    |        natural|
    |cultural_danger|
    +---------------+
    ```

5. Une fois la liste obtenue, utiliser `withColumn()` pour effectuer le remplacement des valeurs.
    ``` python
    df = df.withColumn("Catégorie",\
            when(col("Catégorie").contains('cultural'), 'Culturel')\
            .when(col("Catégorie").contains('mixed'), 'Mixte')\
            .when(col("Catégorie").contains('natural'), 'Naturel')
            )

    df.show(5)
    df.select("Catégorie").distinct().show()
    ```
    _Output :_
    ```
    +---------+---------+--------------------+------------+---+--------------------+--------+
    |     Pays|Catégorie|                Site|Site délisté| ID|                 URL|En péril|
    +---------+---------+--------------------+------------+---+--------------------+--------+
    | Équateur|  Naturel|      Îles Galápagos|       false|  1|https://whc.unesc...|   false|
    | Équateur| Culturel|      Ville de Quito|       false|  2|https://whc.unesc...|   false|
    |Allemagne| Culturel|Cathédrale d'Aix-...|       false|  3|https://whc.unesc...|   false|
    |   Canada| Culturel|Lieu historique n...|       false|  4|https://whc.unesc...|   false|
    |  Tunisie|  Naturel|Parc national de ...|       false|  8|https://whc.unesc...|   false|
    +---------+---------+--------------------+------------+---+--------------------+--------+
    only showing top 5 rows

    +---------+
    |Catégorie|
    +---------+
    |    Mixte|
    | Culturel|
    |  Naturel|
    +---------+
    ```
6. Nous allons créer un nouveau DataFrame réordonné tout en conservant une copie de l'original afin de pouvoir revenir en arrière si nécessaire. Pour ce faire, nous allons utiliser le code suivant :
    ``` python
    df_reorder = df.select(['ID',  'Site', 'Catégorie', 'Site délisté', 'En péril', 'Pays', 'URL']) # nouveau dataframe

    newColumnNames = [x.replace("é","e").lower() for x in df_reorder.columns]
    df_reorder = df_reorder.toDF( *newColumnNames )
    df_reorder.show(5)
    ```
    _Output :_
    ```
    +---+--------------------+---------+------------+--------+---------+--------------------+
    | id|                site|categorie|site deliste|en peril|     pays|                 url|
    +---+--------------------+---------+------------+--------+---------+--------------------+
    |  1|      Îles Galápagos|  Naturel|       false|   false| Équateur|https://whc.unesc...|
    |  2|      Ville de Quito| Culturel|       false|   false| Équateur|https://whc.unesc...|
    |  3|Cathédrale d'Aix-...| Culturel|       false|   false|Allemagne|https://whc.unesc...|
    |  4|Lieu historique n...| Culturel|       false|   false|   Canada|https://whc.unesc...|
    |  8|Parc national de ...|  Naturel|       false|   false|  Tunisie|https://whc.unesc...|
    +---+--------------------+---------+------------+--------+---------+--------------------+
    only showing top 5 rows
    ```
    > **Remarques :** <br>
    Bien que Spark accepte les noms de colonnes commençant par une majuscule, il est généralement préférable de suivre une convention de nommage en minuscules avec des underscores pour améliorer la lisibilité et éviter les problèmes potentiels avec d'autres outils ou systèmes.<br>
    Par contre, Spark n'accepte pas de sauvegarder des tables qui ont des noms de colonnes ayant des caractères spéciaux ou des espaces. Il est donc important de s'assurer que les noms de colonnes sont conformes à ces restrictions lors de la manipulation de données avec Spark. <br><br>
    Pour généraliser le code on aurait pu utilier les **Regex** ou la bibliothèque `unicode` afin de remplacer les lettres qui ont des accents par des lettres sans accents.
7. Avant de sauvegarder le dataframe dans le Lakehouse (comme table delta), on va vérifier que le bon type est associé à chacune des colonnes.
    ``` python
    spark_df = df_reorder
    spark_df.printSchema()
    ```
    _Output :_
    ```json
    root
    |-- id: long (nullable = true)
    |-- site: string (nullable = true)
    |-- categorie: string (nullable = true)
    |-- site deliste: boolean (nullable = true)
    |-- en peril: boolean (nullable = false)
    |-- pays: string (nullable = true)
    |-- url: string (nullable = true)
    ```

    Le type de **id** n'est pas le bon, on va alors préciser le bon type avec la ligne de code suivante :
    ```python
    from pyspark.sql.types import IntegerType, StringType, BooleanType
    spark_df = df_reorder.withColumn("id",col("id").cast(IntegerType())) \
                    .withColumn("site",col("site").cast(StringType())) \
                    .withColumn("categorie",col("categorie").cast(StringType())) \
                    .withColumn("site deliste",col("site deliste").cast(BooleanType())) \
                    .withColumn("en peril",col("en peril").cast(BooleanType())) \
                    .withColumn("pays",col("pays").cast(StringType())) \
                    .withColumn("url",col("url").cast(StringType())) \
                
    spark_df.printSchema()
    ```
    _Output :_
    ```json
    root
    |-- id: integer (nullable = true)
    |-- site: string (nullable = true)
    |-- categorie: string (nullable = true)
    |-- site deliste: boolean (nullable = true)
    |-- en peril: boolean (nullable = false)
    |-- pays: string (nullable = true)
    |-- url: string (nullable = true)
    ```
    > **Remarque :**<br> Les types `long` et `integer` représentent tous deux des entiers, la différence majeure réside dans le nombre de bytes. Avec `integer`, on est en 4-bytes et les valeurs peuvent aller de -2,147,483,648 à 2,147,483,647. En `long` on est sur 8-bytes et les valeurs vont de -9,223,372,036,854,775,808 à 9,223,372,036,854,775,807.<br>
    Pour notre cas, le type `integer` est suffisant.
8. Sauvegarder le dataframe dans le **Lakehouse Silver**
    ``` python
    spark_df = spark_df.toDF( *(column.replace(' ', '_') for column in spark_df.columns) ) # On retire les espaces des noms de colonnes
    spark_df.write.format("delta").mode("overwrite").saveAsTable("DE_LH_Silver.world_heritage_sites")
    ```
    Une fois le code exécuté, aller dans le **Lakehouse Silver** afin de vérifier que la table a été créée.<br>
    <img src="https://github.com/Expertime/exp-data-fabric-workshops/blob/main/Images/Notebook/Notebook-result-whs.png" alt="" width="20%"/> <br>
