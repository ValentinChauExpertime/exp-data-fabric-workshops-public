<link rel="stylesheet" type="text/css" href="styles.css">
 
```
- Transformation de données via fonction anonyme
- Transformation de données via fonction anonyme
- Modification de la structure des colonnes   
```

# Notebook : Transformation de données complexes avec Pandas
Ici on abordera essentiellement des transformations de données demandant un peu plus de manipulations :
- Remplacements de valeurs multiples sur une même colonne
- Utilisation de fonctions custom pour transformer la donnée

# 1. Renommage des colonnes
[Documentation officielle : pandas.DataFrame.rename](https://pandas.pydata.org/docs/reference/api/pandas.DataFrame.rename.html)
``` python
old_columns = [x for x in df.columns.tolist() if x.find("Informations complementaires.")!=-1]
cols_dictio = {}

for x_col in old_columns:
    cols_dictio[ x_col ] = x_col.replace("Informations complementaires.","")
display( cols_dictio )

df = df.rename(columns=cols_dictio)
df.head(2)
```

# 2. Extraire les données liées aux surfaces :
``` python
df["Bien"] = df["Bien"].apply( lambda x : x.replace("ha","").replace(" ","").replace(",",".") if x!=None else None).astype(float)
df["Zone tampon"] = df["Zone tampon"].apply( lambda x : x.replace("ha","").replace(" ","").replace(",",".") if x!=None else None).astype(float)

# df['Bien'].str.replace(r'[^\d,\.]', '', regex=True).replace(",",".") # version avec regex
```

# 3. Conversion des coordonnées géographiques du format DMS au format DD
## 3.1. Explication
Les coordonnées DMS sont largement utilisées dans la cartographie, la navigation et d'autres applications géospatiales où une précision détaillée de la position est essentielle. Ils sont composés de trois parties distinctes : **Degrés (°) - Minutes (') - Secondes ('')**.<br>
*Par exemple, une coordonnée DMS comme 40° 26' 46.8" N indique : 40 degrés de latitude Nord, 26 minutes, 46.8 secondes.*<br>

Dans la carthographie numérique ou les systèmes de géolocalisation, les coordonnées en **DD (Degrés - Décimaux)** sont préférés aux coordonnées DMS parce qu'ils sont plus simples d'utilisation, précis et interopérables. <br>

Pour des besoins de visualisations dans Power BI, nous allons devoir utiliser les coordonnées en **DD** et donc au préalable convertir les coordonnées géographiques au format **DMS** que nous avons actuellement dans notre base de données en coordonnées au format **DD**.<br><br>
### Formule de conversion
$$ 
    DD = \text{Degrés} + \left(\frac{\text{Minutes}}{60}\right) + \left(\frac{\text{Secondes}}{3600}\right)
$$
Exemple :
$$ 
    DMS \ (40^\circ\ 26'\ 46.8''\ N) \rightarrow DD = 40 + \frac{26}{60} + \frac{46.8}{3600} = 40.44633
$$

> Si le point est situé au Sud *(S)* ou à l'Ouest *(W)*, les coordonnées seront exprimées avec une valeur négative.

## 3.2. [Pratique] Réaliser la manipulation sur un dataframe Pandas
On peut réaliser la conversion sur le DataFrame de plusieurs manières, par exemple en utilisant des fonctions lambda, des méthodes de transformation de colonnes, ou des UDF avec PySpark (cf. **Note**). <br>
Dans un premier temps, nous allons créer une fonction pour séparer la latitude de la longitude, et une autre pour effectuer le calcul présenté plus haut.
1. . Créer une fonction pour séparer la latitude de la longitude `separate_DMS_latitude_from_longitude`
    ```python
    def separate_DMS_latitude_from_longitude(input_str):
        """
            Séparer une chaine de caractères contenant les coordonnées en dms de la latitude et longitude en deux

            Args:
                x (str): Coordonnées à séparer en deux (2).
                Format [Degres Minutes Secondes Point_Cardinal_Latitude Degres Minutes Secondes Point_Cardinal_Longitude]

            Returns:
                result (list) : Liste de deux (2) chaînes de caractères, 
                où le premier élément est la latitude et le deuxième la longitude.

            Example:
            >>> separate_DMS_latitude_from_longitude('0 41 23.496 N 90 30 4.748 W')
            ['0 41 23.496 N', '90 30 4.748 W']
        """
        #result = None
        if type(input_str)==str:
            input_str_split = input_str.split(" ")
            result = [" ".join(input_str_split[:4]), " ".join(input_str_split[4:])]
        else :
            result = None
        
        return result
    init_coords = '0 41 23.496 N 90 30 4.748 W'
    print( f"Coordonnées \"{init_coords}\" deviennent {separate_DMS_latitude_from_longitude(init_coords)}")
    ```
    _Output :_
    ```
    Coordonnées "0 41 23.496 N 90 30 4.748 W" deviennent ['0 41 23.496 N', '90 30 4.748 W']
    ```
2. Créer une fonction de conversion `dms_to_dd`
    ```python
    def dms_to_dd(dms_str):
        # Separer dms_str par l'espace
        parts = dms_str.split() if dms_str!=None else [0,0,0,'']
        
        # Extraire les degrees, minutes, secondes et la direction
        degrees = float(parts[0])
        minutes = float(parts[1])
        seconds = float(parts[2])
        direction = parts[3]
        
        # Convertir coordonnees DMS en decimal degrees
        dd = degrees + minutes / 60 + seconds / 3600
        
        # Coordonnees negatives si Sud ou Ouest
        if direction in ['S', 'W']:
            dd *= -1
        
        return round(dd,4)
    lat = '0 41 23.496 N'
    print( f"{lat} (en DMS) devient {dms_to_dd(lat)} (en DD)")
    ```
    _Output :_
    ```
    0 41 23.496 N (en DMS) devient 0.6899 (en DD)
    ```
2. Appliquer les fonctions `dms_to_dd` au dataframe<br>
    ``` python
    df["Latitude"] = df["Coordonnées_en_DMS"].apply(lambda x : dms_to_dd(separate_DMS_latitude_from_longitude(x)[0]) if x!=None else None)
    df["Longitude"] = df["Coordonnées_en_DMS"].apply(lambda x : dms_to_dd(separate_DMS_latitude_from_longitude(x)[1]) if x!=None else None)
    ```

> **Note:** <br>
    Les fonctions **lambda** et les **UDF** (User Defined Functions) diffèrent principalement en termes d'utilisation et de performance.<br> 
    Les fonctions **lambda** sont des fonctions anonymes, idéales pour des transformations simples et rapides sur de petits ensembles de données, généralement utilisées avec Pandas. <br>
    Les **UDF**, en revanche, sont définies par l'utilisateur pour des transformations complexes sur de grands ensembles de données dans PySpark. Elles permettent de traiter des données distribuées, mais peuvent être moins performantes que les fonctions natives de Spark en raison de la sérialisation des données (*processus de conversion d'un objet en un format qui peut être facilement stocké ou transmis, et est nécessaire pour passer les données entre Python et le moteur Spark*).

# 4. Autres transformations
Avant de sauvegarder le dataframe sous forme de table delta, nous allons :
- garder quelques colonnes
- changer l'ordre des colonnes 
- renommer "encore" quelques colonnes
- remplacer les lettres accentuées par des lettres sans accent
```python
colonne_a_garder = ["Source", 'ID', 'Dossier', 'Site', 'Description', "Date d'inscription", 'Latitude', 'Longitude', 'Bien', 'Zone tampon', 'Critères', 'Date de retrait de la Liste']
df_reorder = df[ colonne_a_garder ]
df_reorder = df_reorder.rename(columns = {"Bien":"Superficie en ha", "Zone tampon":"Zone tampon en ha"})
df_reorder.columns = [x.replace("è","e").replace("'","").lower() for x in df_reorder.columns]
print( df_reorder.columns.tolist() )
```
_Output :_
```
['source', 'id', 'dossier', 'site', 'description', 'date dinscription', 'latitude', 'longitude', 'superficie en ha', 'zone tampon en ha', 'criteres', 'date de retrait de la liste']
```

# 5. Sauvegarder le dataframe dans le Lakehouse Silver
## 5.1. Conversion du dataframe Pandas en dataframe Spark
``` python 
spark_df = spark.createDataFrame(df_reorder)
spark_df.printSchema()
```
_Output :_
```
root
 |-- id: long (nullable = true)
 |-- dossier: string (nullable = true)
 |-- site: string (nullable = true)
 |-- description: string (nullable = true)
 |-- date dinscription: string (nullable = true)
 |-- latitude: double (nullable = true)
 |-- longitude: double (nullable = true)
 |-- superficie en ha: float (nullable = true)
 |-- zone tampon en ha: float (nullable = true)
 |-- criteres: string (nullable = true)
 |-- date de retrait de la liste: string (nullable = true)
```
## 5.2. Conversion du type des colonnes
On l'a vu dans la requête `spark_df.printSchema()`, le type de donnée de certaines colonnes n'est pas le bon. Avant de créer la table delta dans le Lakehouse, il faudrait affecter le bon type de données à la colonne en question. Pour cela, on utilise `withColumn`, `col` et `cast`.
``` python 
spark_df = spark.createDataFrame(df_reorder)
spark_df = spark_df.withColumn("id",col("id").cast(IntegerType())) \
                .withColumn("dossier",col("dossier").cast(StringType())) \
                .withColumn("site",col("site").cast(StringType())) \
                .withColumn("date dinscription",col("date dinscription").cast(IntegerType())) \
                .withColumn("latitude",col("latitude").cast(DoubleType())) \
                .withColumn("longitude",col("longitude").cast(DoubleType())) \
                .withColumn("superficie en ha",col("superficie en ha").cast(DoubleType())) \
                .withColumn("zone tampon en ha",col("zone tampon en ha").cast(DoubleType())) \
                .withColumn("criteres",col("criteres").cast(StringType())) \
                .withColumn("date de retrait de la liste",col("date de retrait de la liste").cast(IntegerType()))
            
spark_df.printSchema()
```
_Output :_
```
root
 |-- id: integer (nullable = true)
 |-- dossier: string (nullable = true)
 |-- site: string (nullable = true)
 |-- description: string (nullable = true)
 |-- date dinscription: integer (nullable = true)
 |-- latitude: double (nullable = true)
 |-- longitude: double (nullable = true)
 |-- superficie en ha: double (nullable = true)
 |-- zone tampon en ha: double (nullable = true)
 |-- criteres: string (nullable = true)
 |-- date de retrait de la liste: integer (nullable = true)
```
## 5.3. Création de la table delta
``` python 
spark_df = spark_df.toDF( *(column.replace(' ', '_') for column in spark_df.columns) ) # On retire les espaces des noms de colonnes
spark_df.write.format("delta").mode("overwrite").saveAsTable("DE_LH_Silver.world_heritage_sites_details_spark")
```
Une fois le code exécuté, aller dans le Lakehouse Silver afin de vérifier que la table a été créée.

# Documentations officielles
- [pandas.DataFrame.apply](https://pandas.pydata.org/docs/reference/api/pandas.DataFrame.apply.html)
- [pyspark.sql.functions.pandas_udf](https://spark.apache.org/docs/3.1.2/api/python/reference/api/pyspark.sql.functions.pandas_udf.html)
- [Apache Spark > Data Types](https://spark.apache.org/docs/latest/sql-ref-datatypes.html) (si vous descendez un peu plus bas dans le lien vous trouverez les équivalents de types entre Python et Spark)


# Brouillon
``` python
old_columns = [x for x in df.columns.tolist() if x.find("Informations complementaires.")!=-1]
#old_columns = ["Informations complementaires.Date d'inscription", 'Informations complementaires.Significant modifications to the boundaries', 'Informations complementaires.Critères', 'Informations complementaires.Bien', 'Informations complementaires.Dossier', 'Informations complementaires.Coordonnées_en_DMS', 'Informations complementaires.Zone tampon', "Informations complementaires.Année d'inscription de la modification mineure des limites", 'Informations complementaires.Date de retrait de la Liste']
#new_columns = [x.replace("Informations complementaires.","") for x in old_columns]

cols_dictio = {}
for x_col in old_columns:
    #cols_dictio[ old_columns[i] ] = new_columns[i]
    cols_dictio[ x_col ] = x_col.replace("Informations complementaires.","")
display( cols_dictio )

df = df.rename(columns=cols_dictio)
df.head(2)
```
